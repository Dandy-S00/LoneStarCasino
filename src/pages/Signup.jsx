import { useState } from 'react';
import { Link,useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import Navbar from '../components/Navbar';
import Footer from '../components/Footer';
export default function Signup(){const {signUp,signIn}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [dateOfBirth,setDob]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');try{if(password.length<8)throw new Error('Password must be at least 8 characters.');const age=(Date.now()-new Date(dateOfBirth).getTime())/31557600000;if(!dateOfBirth||age<18)throw new Error('You must be at least 18 years old to create an account.');await signUp({email:email.trim(),password,dateOfBirth});await signIn({email:email.trim(),password});nav('/games',{replace:true});}catch(err){setError(err.message||'Could not create account.');}finally{setBusy(false);}}return <><Navbar showDeposit={false}/><section className="deposit-section"><div className="container"><div className="deposit-form-card" style={{maxWidth:480,margin:'0 auto'}}><h2 className="section-title">Create Account</h2><form onSubmit={submit}><input className="form-control mb-3" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="form-control mb-3" type="date" value={dateOfBirth} onChange={e=>setDob(e.target.value)} required/><input className="form-control mb-3" type="password" placeholder="At least 8 characters" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn-texas w-100" disabled={busy}>{busy?'Creating account...':'Create My Account'}</button>{error&&<div className="error-box mt-3">{error}</div>}</form><p className="text-center mt-3">Already registered? <Link to="/login">Log in</Link></p></div></div></section><Footer minimal/></>;} 
