import { useState } from 'react';
import { Link,useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import Navbar from '../components/Navbar';
import Footer from '../components/Footer';
export default function Login(){const {signIn}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');try{await signIn({email:email.trim(),password});nav('/games',{replace:true});}catch(err){setError(err.message||'Could not sign in.');}finally{setBusy(false);}}return <><Navbar showDeposit={false}/><section className="deposit-section"><div className="container"><div className="deposit-form-card" style={{maxWidth:480,margin:'0 auto'}}><h2 className="section-title">Log In</h2><form onSubmit={submit}><input className="form-control mb-3" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="form-control mb-3" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn-texas w-100" disabled={busy}>{busy?'Signing in...':'Log In'}</button>{error&&<div className="error-box mt-3">{error}</div>}</form><p className="text-center mt-3">New here? <Link to="/signup">Create an account</Link></p></div></div></section><Footer minimal/></>;} 
